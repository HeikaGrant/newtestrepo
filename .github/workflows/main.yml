name: myfirstworkflow

on: 
  workflow_dispatch:  

jobs:
  job2:
      runs-on: ubuntu-latest
      steps:
        - name: Ship it
          uses: mscoutermarsh/cowsays-action@master
          with:
            text: ship ship ship!
            color: red
   
