on: workflow_dispatch
jobs:
  first_demo_job:
    name: job1
    outputs:
      decription:"job1"
  second_demo_job:
   cow:
      name: Cowsays
      runs-on: ubuntu-latest
      steps:
      - name: Ship it
        uses: mscoutermarsh/cowsays-action@master
        with:
          text: 'ship ship ship!'
          color: 'red'
